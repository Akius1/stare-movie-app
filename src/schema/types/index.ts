export type userType = {
  email: string;
  name: string;
  password: string;
};

export type filmType = {
  name: string;
  description: string;
  release_date: string;
  ticket_price: number;
  country: string;
  genre: string;
  image_links: string;
};
